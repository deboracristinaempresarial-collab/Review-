{
  "name": "aruna-review-shopify",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Aruna Review Shopify app with native App Home and Theme App Extension",
  "scripts": {
    "deploy": "shopify app deploy --config production --allow-updates",
    "dev": "shopify app dev --config production"
  },
  "dependencies": {
    "@shopify/ui-extensions": "2026.7.x",
    "preact": "^10.27.0"
  },
  "engines": {
    "node": ">=22"
  }
}
